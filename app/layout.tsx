import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Vintage Car Dealership - Classic Cars & Restoration',
  description: 'Discover rare vintage cars and professional restoration services. Expert team specializing in classic American muscle cars and European sports cars.',
  keywords: 'vintage cars, classic cars, restoration, muscle cars, sports cars, dealership',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}